# 复制为 .env.local 后按需修改（手动运行 npm run dev 时必填 BACKEND_URL，须与 uvicorn 端口一致）。
# start-dashboard.ps1 会为子进程设置 BACKEND_URL；若后端因端口占用改用其它端口，请与此处对齐。
# 可参考 voc-data-product/runtime/.run/status.json 中的 backend_url / backend_port。
BACKEND_URL=http://127.0.0.1:8000

# 可选：浏览器直连后端 API（跨域时需后端 CORS 已放行）。默认留空使用同源 /api 由 Next rewrites 转发。
# NEXT_PUBLIC_API_BASE=
